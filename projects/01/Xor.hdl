CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    And(a= a, b= b, out= aab);
    Not(in= a, out=nota );
    Not(in= b, out= notb);
    And(a= nota, b= notb, out= naanb);
    Or(a=naanb , b= aab, out= orr);
    Not(in= orr, out= out);
}
